import { NextPage } from "next";

import { HomeContainer } from "@/containers";

const Home: NextPage = () => {
    return (
        <div>
            <HomeContainer />
        </div>
    );
};

export default Home;
